import 'package:flutter/material.dart';

class ValidationGetx2 extends StatelessWidget {
  const ValidationGetx2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Validated')),
    );
  }
}
