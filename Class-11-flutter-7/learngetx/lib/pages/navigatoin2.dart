import 'package:flutter/material.dart';

class NavSecond extends StatelessWidget {
  const NavSecond({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Second screen')),
    );
  }
}
