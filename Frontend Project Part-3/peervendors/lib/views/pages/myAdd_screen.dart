import 'package:flutter/cupertino.dart';

class MyAddScreen extends StatelessWidget {
  const MyAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('myAdd Screen'),
    );
  }
}
