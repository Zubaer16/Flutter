import 'package:flutter/cupertino.dart';

class AddButtonScreen extends StatelessWidget {
  const AddButtonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AddButton Screen'),
    );
  }
}
